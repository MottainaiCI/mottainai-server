{{template "base/head" .}}
{{template "base/menu" .}}

        <div class="content mt-3">
            <div class="animated fadeIn">
                <div class="row">

                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header">
                            <strong class="card-title">Tasks list</strong>
                        </div>
                        <div class="card-body">
                          {{template "tasks/single" .}}
                        </div>
                    </div>
                </div>

		{{template "base/footer" .}}
